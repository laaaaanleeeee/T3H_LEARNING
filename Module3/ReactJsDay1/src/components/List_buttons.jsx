import React from "react";

const List_buttons = (props) => {
  return (
    <button type="button" className="buton_cato">
      {props.title_button}
    </button>
  );
};

export default List_buttons;
